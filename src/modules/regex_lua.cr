require "lua"

class RegexLua
    include LuaCallable

    def initialize
        
    end

    # Match regex with text
    def match (regex_str : String, input : String) : Nil
        # todo: Add the code
    end

    # exec regex with text
    def exec (regex_str : String, input : String) : Nil
        # todo: Add the code
    end
end